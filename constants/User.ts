
/**
 * ROLE_CLASS
 * ----------
 * This const variable is a readyonly variable for genearl classification of the users.
 * `admin`: For System Admin category, which is hard-coded into the system.
 * `user`: For General Users, those which sign-up or invited into the system.
 */
export const ROLE_CLASS = ["admin", "user"] as const; 